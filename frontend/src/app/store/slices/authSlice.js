import authReducer, {
  setCredentials,
  logout,
  setAuthLoading,
  setAuthError,
  clearAuthError,
} from '../../../features/auth/authSlice';

export {
  setCredentials,
  logout,
  setAuthLoading,
  setAuthError,
  clearAuthError,
};

export default authReducer;
