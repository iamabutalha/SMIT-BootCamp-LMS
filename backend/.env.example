# ---- Server ----
NODE_ENV=development
PORT=5000
# Comma-separated list of allowed frontend origins (or * in dev)
CORS_ORIGIN=http://localhost:5173

# ---- Database ----
MONGODB_URI=mongodb://127.0.0.1:27017/smit_lms

# ---- Auth (JWT) ----
JWT_SECRET=replace_me_with_a_long_random_string
JWT_EXPIRES_IN=7d
